SET QUOTED_IDENTIFIER ON;GOIF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID('dbo.employees') AND type = 'U')BEGIN CREATE TABLE dbo.employees(  client_id            NVARCHAR(50)    NOT NULL,  emp_id               NVARCHAR(100)   NOT NULL,  site_name            NVARCHAR(200)   NOT NULL,  department           NVARCHAR(200)   NOT NULL,  emp_role             NVARCHAR(200)   NOT NULL,   work_start           DATETIME        NOT NULL);ENDGO